import 'package:flutter/material.dart';

class AppSpecialColor {
  AppSpecialColor._();

  // Greens
  static const green = Color(0xFF2E7D32);
  static const darkGreen = Color(0xFF1B5E20);

  // Browns
  static const brown = Color(0xFF6D4C41);
  static const coffee = Color(0xFF3E2723);

  // Reds
  static const red = Color(0xFFB71C1C);
  static const darkRed = Color(0xFF8E0000);
  static const wineRed = Color(0xFF880E4F);

  // Blues
  static const darkBlue = Color(0xFF01579B);
  static const indigoBlue = Color(0xFF1A237E);

  // Teal / Aqua
  static const darkTeal = Color(0xFF00695C);
  static const aqua = Color(0xFF007C91);

  // Yellow / Gold
  static const amber = Color(0xFFFF8F00);
  static const gold = Color(0xFFC49000);

  // Purple
  static const plum = Color(0xFF8E24AA);
  static const indigo = Color(0xFF4527A0);

  // Pink
  static const darkPink = Color(0xFFC2185B);
  static const magenta = Color(0xFF880E4F);

  // Lista completa (útil para picker)
  static const all = [
    green,
    darkGreen,
    brown,
    coffee,
    red,
    darkRed,
    wineRed,
    darkBlue,
    indigoBlue,
    darkTeal,
    aqua,
    amber,
    gold,
    plum,
    indigo,
    darkPink,
    magenta,
  ];
}
